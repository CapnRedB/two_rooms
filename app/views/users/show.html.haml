%p
  Email
  .email
    %strong= @user.email
%p
  Name
  .name
    %strong= @user.name
%p
  Signed in at
  .last_sign_in
    %strong= @user.last_sign_in_at
%p
  Sign in count
  .count
    %strong= @user.sign_in_count

- if current_user = @user
  = link_to "Log out", destroy_user_session_path, method: "delete", class: 'btn btn-danger'