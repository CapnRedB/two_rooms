<div class="container-fluid text-center">
	<div class="row">
		<div class="col-xs-12">
<<<<<<< HEAD
			Join code: {{#link-to "game.code"}}{{model.code}}{{/link-to}}
		</div>
	</div>
=======
			Join code: {{#link-to "game.code" model}}{{model.code}}{{/link-to}}
		</div>
	</div>

	<div class="row">
		<div class="col-xs-6">
			<div>
			</div>
			<table class="table">
				<thead>
					<tr>
						<th>Player</th>
						<th></th>
					</tr>
				</thead>
				<tbody>
					{{#each model.game_player as |game_player|}}
						<tr>
							<td>{{game_player.name}}</td>
							<td>
								{{#if game_player.is_self}}
          							{{confirm-button action="leaveGame" label="Leave" param=game_player}}
								{{else}}
									{{#if model.is_owned}}
										{{confirm-button action="leaveGame" label="Kick" param=game_player}}
									{{/if}}
								{{/if}}
							</td>
						</tr>
					{{/each}}
				</tbody>
			</table>

		</div>
	</div>

>>>>>>> feature-game-um
</div>